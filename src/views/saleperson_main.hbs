<head>
  <link rel="stylesheet" type="text/css" href="main.css" />
  <link
    href="https://fonts.googleapis.com/icon?family=Material+Icons"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
  <script>
    const logout = () => {window.location.href="http://localhost:3000/logout"};
    const move = () => {window.location.href="http://localhost:3000/sugang"};
  </script>
</head>
<body>
    <h1>딜러 정보</h1>
    <h3>이름 : {{Sname}} 이메일 : {{Semail}}</h3>

    <h1> 자동차 목록 조회 </h1>
    {{#each registered_car}}
    <table>
      <tr>
        <td>{{Vin}}</td>
        <td>{{Price}}</td>
      </tr>
    </table>
    {{/each}}

    <h2><a href="/admin/select_booking">예약 정보 조회</a></h2>
    <h2><a href="/admin/edit_booking">예약 정보 수정</a></h2>
    <div>
    <table>
      <form method="post" action="/admin/update/reservation" required>
        <tr>
          <td>예약 번호</td>
          <td>
            <input list="saleid" name="saleid">
              <datalist id="saleid">
                {{#each customers}}
                <option>{{Sale_Id}}</option>
                {{/each}}
              </datalist>
          </td>
        </tr>
        <tr>
            <td>Vin</td>
            <td>
                <input list="newvin" name="newvin">
                    <datalist id="newvin">
                        {{#each vins}}
                        <option value={{SVin}}>
                        {{/each}}
                    </datalist>
            </td>
        </tr>
        <tr>
            <td>예약 고객</td>
            <td>
                <input list="newcustomer" name="newcustomer" required>
                    <datalist id="newcustomer">
                        {{#each customers}}
                        <option>{{Cname}}, {{Cemail}}</option>
                        {{/each}}
                    </datalist>
            </td>
        </tr>
        <tr>
            <td>판매원</td>
            <td>
                {{#each salesperson}}
                {{Sname}}, {{Semail}}
                {{/each}}
            </td>
        </tr>
        <tr>
            <td>예약날짜</td>
            <td><input type="date" name="bookdate" required></td>
        </tr>
        <tr>
            <td><input type="submit" value="수정"></td>
        </tr>
      </form>
    </table>
    </div>
    <h2>차량 등록 정보 조회</h2>
    <h2>차량 등록 정보 수정/삭제</h2>
    <h2>차량 정보 조회</h2>
    <h2>차량 정보 수정/삭제</h2>
</body>